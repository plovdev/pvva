{
  "base-url": "http://4porno365.run",
  "main": {
    "supports": true,
    "endpoint": "/${page}",
    "main-search": {
      "supports": true,
      "endpoint": "/search/${user-input}/popular/${page}"
    }
  },
  "models": {
    "supports": true,
    "endpoint": "/models/${page}",
    "model": {
      "supports": true,
      "endpoint": "/models/${model-name}"
    },
    "model-search": {
      "supports": true,
      "endpoint": "/?do=ajax&action=searchModel&model=${user-input}"
    }
  },
  "categories": {
    "supports": true,
    "endpoint": "/categories",
    "category": {
      "supports": true,
      "endpoint": "/categories/${category}"
    }
  },
  "video": {
    "supports": true,
    "endpoint": "/movie/${video-id}"
  },
  "mirrors": {
    "supports": true,
    "urls": [
      "http://5porno365.run",
      "http://6porno365.run",
      "http://7porno365.run"
    ]
  }
}