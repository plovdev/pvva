{
    "base-url": "http://9porno365.info",
    "main": {
        "supports": true,
        "endpoint": "/${page}",
        "main-search": {
            "supports": true,
            "endpoint": "/search/${user-input}/popular/${page}"
        }
    },
    "models": {
        "support": true,
        "endpoint": "/models/${page}",
        "model": {
            "supports": true,
            "endpoint": "/models/${model}"
        },
        "model-search": {
            "supports": true,
            "endpoint": "/?do=ajax&action=searchModel&model=${user-input}"
        }
    },
    "categories": {
        "supports": true,
        "endpoint": "/categories/${page}",
        "category": {
            "supports": true,
            "endpoint": "/categories/${category}"
        }
    },
    "video": {
        "supports": true,
        "endpoint": "/movie/${video-id}"
    },
    "mirrors": {
        "supports": true,
        "urls": [
            "http://my.porno365.bike",
            "http://5porno365.net",
            "http://1porno365.info"
        ]
    }
}