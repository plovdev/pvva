{
  "base-url": "https://sex-students.ru",
  "main": {
    "supports": true,
    "endpoint": "/${page}",
    "main-search": {
      "supports": true,
      "endpoint": "/search/${user-input}"
    }
  },
  "models": {
    "supports": true,
    "endpoint": "/models",
    "model": {
      "supports": true,
      "endpoint": "/models/${model}"
    },
    "model-search": {
      "supports": true,
      "endpoint": "/models/search/${user-input}"
    }
  },
  "categories": {
    "supports": true,
    "endpoint": "/categories",
    "category": {
      "supports": true,
      "endpoint": "/categories/${category}"
    }
  },
  "video": {
    "supports": true,
    "endpoint": "/video/${video-id}"
  },
  "mirrors": {
    "supports": false
  }
}